<script setup lang="ts">
// Short instructions for canvas interaction and zoom/pan.
</script>

<template>
  <p class="hint-banner hint-banner--compact">
    <strong>Source / Segmentation images:</strong> left → FG, right → BG, longer drag → box (dots/box drawn only on
    Source). Coords: x →, y ↓.
    <strong>Zoom / pan:</strong> ⌘/Ctrl + scroll on either image (toward cursor); when zoomed, scroll = vertical pan,
    Shift+scroll = horizontal — same position and scale on both columns.
  </p>
</template>
